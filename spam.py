import pyautogui as spam
import time

msg = input("Enter Message: ")

count = 0
limit = int(input("Jumlah: "))

time.sleep(2)

# while i<int(limit):
    while count<int(limit)
    count+=1
    if count > limit:
        break
        spam.typwriter(msg)
        spam.press('Enter')